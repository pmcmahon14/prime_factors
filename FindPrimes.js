const upperlimit = 1000;
let primeArray = [2];

//TODO DETERMINE UPPER LIMIT
//TODO BUILD FOR LOOP THAT STOPS AT SQUARE ROOT
//TODO IF PRIME, PUSH INTO ARRAY

function findPrime () {
    if ()
    for (let n=2; sq = num<Math.floor(sqrt(num)); n++) {

    }
}

